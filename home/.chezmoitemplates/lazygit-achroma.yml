# achroma theme fragment for lazygit, layered on top of config.yml via
# LG_CONFIG_FILE (comma-separated configs; later files override earlier ones).
# The variant is picked at shell start from ACHROMA_VARIANT and refreshed by
# the `theme` command, so no applied file is ever edited in place.
gui:
  authorColors:
    "noidilin": "{{ .c.acc06 }}"
    "*": "{{ .c.mono17 }}"
  theme:
    activeBorderColor:
      # Border color of focused window
      - "{{ .c.mono19 }}"
      - bold
    inactiveBorderColor:
      # Border color of non-focused windows
      - "{{ .c.mono13 }}"
    searchingActiveBorderColor:
      # Border color of focused window when searching in that window
      - "{{ .c.acc08 }}"
      - bold
    optionsTextColor:
      # Color of keybindings help text in the bottom line
      - "{{ .c.mono11 }}"
    selectedLineBgColor:
      # Background color of selected line.
      - "{{ .ink.selectionBg }}"
    inactiveViewSelectedLineBgColor:
      # Background color of selected line when view doesn't have focus.
      - bold
    cherryPickedCommitFgColor:
      # Foreground color of copied commit
      - "{{ .c.acc07 }}"
    cherryPickedCommitBgColor:
      # Background color of copied commit
      - "{{ .c.mono09 }}"
    markedBaseCommitFgColor:
      # Foreground color of marked base commit (for rebase)
      - "{{ .c.acc08 }}"
    markedBaseCommitBgColor:
      # Background color of marked base commit (for rebase)
      - "{{ .c.mono13 }}"
    unstagedChangesColor:
      # Color for file with unstaged changes
      - red
    defaultFgColor:
      # Default text color
      - "{{ .c.mono17 }}"
