pkgs:
  pacman:
    # Shared packages for BOTH WSL2 and native Arch

    # Shell & CLI tools
    - "nushell"
    - "github-cli"
    - "git-delta"

    # Build essentials
    - "sudo"
    - "which"
    - "pacman-contrib"
    - "base-devel" # gcc, make, etc.

    # Terminal tools
    - "man-db"
    - "man-pages"
