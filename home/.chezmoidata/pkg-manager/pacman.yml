pkgs:
  pacman:
    # common
    - "nushell"
    - "github-cli"
    - "git-delta"
    - "code"

    # specific
    - "sudo"
    - "which"
    - "pacman-contrib"
    - "base-devel" # gcc, make, etc. (may already be installed)
