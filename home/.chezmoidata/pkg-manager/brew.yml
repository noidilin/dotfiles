# macOS-specific packages (Homebrew)
# These packages are only for macOS and use Homebrew-specific names

pkgs:
  brew:
    - "cleanshot" # screen capture + record, key cast
    - "homerow" # vim mode in mac (flash, HJKL)
    - "karabiner-elements"
    # - "menubarusb" # display conntected usb device spec
    # - "ogdesign-eagle" # image reference
    # - "proxy-audio-device" # control external audio interfaces with macOS system volume control
    # - "waves-central" # VSA plugins

    # TODO: add support to these
    # brew install --cask nikitabobko/tap/aerospace
    # brew install --cask TheBoredTeam/boring-notch/boring-notch --no-quarantine
    # not in homebrew [blender launcher](https://github.com/Victor-IX/Blender-Launcher-V2)
