# macOS-specific packages (Homebrew)
# These packages are only for macOS and use Homebrew-specific names

pkgs:
  brew:
    # common
    - "nushell"
    - "gh"
    - "git-delta"
    - "visual-studio-code"
    - "mise"
