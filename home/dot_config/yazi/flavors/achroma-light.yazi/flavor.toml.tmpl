{{- /* Light variant of the achroma.yazi flavor. Rendered from the shared
   role-based template; icon overrides live in the shared template too, so
   they follow the variant. */ -}}
{{- template "yazi-achroma-flavor.toml" (dict "c" .color.achroma.light) -}}
