{{- /* Prompt frame steps to mono10: the mirrored mono07 is nearly invisible on
   the light base because the light ramp is compressed at the background end. */ -}}
{{- template "starship-achroma.toml" (dict
      "c" .color.achroma.light
      "ink" (dict "frame" .color.achroma.light.mono10)) -}}
