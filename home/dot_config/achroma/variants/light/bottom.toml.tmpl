{{- /* Borders step to mono10: the mirrored mono07 is nearly invisible on the
   light base because the light ramp is compressed at the background end. */ -}}
{{- template "bottom-achroma.toml" (dict
      "c" .color.achroma.light
      "ink" (dict "border" .color.achroma.light.mono10 "selectionBg" .color.achroma.light.mono09)) -}}
