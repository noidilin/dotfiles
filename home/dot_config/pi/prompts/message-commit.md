---
description: write commit message in the requested language
argument-hint: "<language>"
---

Write a concise commit message in $ARGUMENTS, for the current staged files, or the last committed files if in detached HEAD mode.
The messages should follow the rules of conventional commits and the commitlint configuration (if it exist).
Use message body to explain the architectural decisions made in this commit (explain why instead of how).
